spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: visits-service
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://tfm-config-server-service:8888
